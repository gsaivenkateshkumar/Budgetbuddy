# @budget-buddy/shared

Shared TypeScript types and constants used by `apps/web` (and future clients,
e.g. a future mobile app). Populated starting in the phase where the web
frontend needs types that mirror the backend's API contracts.
